# Test temporary files (but keep the directory structure)
tmp/**
!tmp/
!tmp/.gitkeep

# Claude Code local settings
.claude/
CLAUDE.local.md

# Ruby dependencies
vendor/
.bundle/

# RSpec output
.rspec_status

Gemfile.lock